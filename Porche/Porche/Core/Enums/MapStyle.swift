import Foundation

/// Tip karte (topologija): standard, flyover, hybrid, satellite.
enum MapTerrainStyle: String, CaseIterable {
    case standard
    case flyover
    case hybrid
    case satellite
}
